.DS_Store
.ruff_cache

# Python cache and virtual environments
__pycache__/
*.py[cod]
.venv/
venv/
env/
inspo/
.pytest_cache/
.mypy_cache/

# Pipeline runtime outputs
data/
logs/
dist/
sources/free-exercise-db/runs/

# Pipeline runtime artifacts (not committed)
pipeline/pipeline.db
pipeline/backups/
pipeline/artifacts/
pipeline/exports/
pipeline/releases/
pipeline/gemini_cache_id.txt
pipeline/batch_job_id.txt
pipeline/batch_manifest.json

# SQLite WAL transient files
pipeline/pipeline.db-shm
pipeline/pipeline.db-wal
*.db-shm
*.db-wal

# graph.ttl is committed — it's the source of truth for the static site deploy

# Site generated data (built by feg_site.py / CI)
site/data.json
site/vocab.json

# Local environment files
.env
.env.*

# Notebooks and working files
*.ipynb
!graph_explorer.ipynb
!pipeline_playbook.ipynb

# Log files
*.log

# Quality reports (derived)
quality_report.csv
quality_history.csv
